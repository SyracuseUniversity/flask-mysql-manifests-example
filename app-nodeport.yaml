apiVersion: v1
kind: Service
metadata:
  labels:
    app: flask-app
  name: flask-app-nodeport
spec:
  ports:
  - port: 8443
    nodePort: 31080
    protocol: TCP
    targetPort: 8443
  selector:
    app: flask-app
  type: NodePort